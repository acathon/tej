# TEJ-Fisc Frontend

Angular 19 with standalone components, signals, and Tailwind CSS.

Structure:
- `src/app/core`: application-wide services, interceptors, auth.
- `src/app/features`: domain features (uploader, extraction, compliance).
- `src/app/shared`: reusable UI components.
